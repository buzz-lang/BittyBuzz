function init_swarm() {
	s = swarm.create(1)
	s.join()
}

function init() {
	init_swarm()
}

function step() {
	s.exec(takeoff)
	#takeoff()
}

function destroy() {

}

# ========================================
# =         CLOSURE DEFINITIONS          =
# ========================================

function takeoff() {
	motorsEnable = 1
}
